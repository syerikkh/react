import React from 'react'

export const Skill = (props) => {
    return (
        <div class="flex flex-col items-center">
            {props.icon}
            <p1>{props.title}</p1>
        </div>
    )
}


